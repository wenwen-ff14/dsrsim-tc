# 0.4.0.6 校正紀錄

## 實戰事件

來源：[使用者提供的 FF Logs，第 42 場、P2 敵方事件](https://www.fflogs.com/reports/V4F6z9GCthdf2Ppq?fight=42&type=damage-done&hostility=1&phase=2&view=events)。
透過已完成真人驗證的瀏覽器，讀取 Casts 與 Damage Done 事件表。
下表時間沿用該頁的 P2 時間顯示。施放事件和傷害套用分開記錄，不能直接當作物件生成時間。

| 事件 | 施放／準備 | 首筆傷害 |
| --- | --- | --- |
| 聖杖讀條完成 | 01:37.663 | — |
| 嚴冬風暴 | 02:09.467 | 02:09.779 |
| 第一輪信仰塔 | 02:13.714 | 02:13.937 |
| 隕石第 1 組 | 02:15.145 | 02:15.859 |
| 隕石第 2 組 | 02:16.577 | 02:17.293 |
| 隕石第 3 組 | 02:18.008 | 02:18.724 |
| 隕石第 4 組 | 02:19.439 | 02:20.155 |
| 隕石第 5 組 | 02:20.872 | 02:21.588 |
| 隕石第 6 組 | 02:22.304 | 02:23.019 |
| 隕石第 7 組 | 02:23.735 | 02:24.450 |
| 堅定信仰 | 02:24.094 | — |
| 第二輪信仰塔 | 02:27.004 | — |

練習時間以既有第一輪塔 43.1 秒為基準，隕石施放偏移取自此表，四捨五入至百分之一秒。
保留其他尚未完整重播的練習流程。FF Logs 頁面另提示紀錄插件不相容或有錯誤、排名失效，
因此這份報告作為本輪時序校正參考，不視為完整、無誤的原生重播資料。

## 繁中遊戲資源

- 冰圈：EObj `0x1EB682` → `sgvf_r1fz_b2419.sgb`，AVFX 物件的 XYZ 縮放均為 `1.3`。
- 火圈：EObj `0x1EB686` → `sgvf_r1fz_b2420.sgb`，AVFX 物件的 XYZ 縮放均為 `1.4`。
- 技能 25575、28591 的 EffectRange 均為 7。外觀縮放與判定半徑分開處理。
- 隕石技能 25577 的 AnimationEnd 2772 指向 `chara/action/mon_sp/m0106/mon_sp003.tmb`。
- 該 TMB 引用 `sound/vfx/monster2/SE_Vfx_Monster_Comet_InsekiRakka_c.scd`，並引用 `m0106sp_03c1m.avfx`、`m0106sp_03c0m.avfx`。

隕石提前建立並等待模型載入，再發送原生技能演出，避免在新建物件尚未準備完成時立即播放。
並未另加 UI 提示音冒充落地音效，亦未匯出或散布遊戲音檔。

## Crash 調查限制

本機三份 dump（程序 42952、124616、94392）均為 `0xC0000374`。
以 minidump 例外執行緒 context、記憶體範圍與 module list 讀取堆疊位址，三份均出現 LGHUB LED 模組；
其中兩份包含相同的 `sdk_legacy_led_x64.dll+0x9854/+0x60711/+0xD1C46/+0x5C384` 位址序列。
這是堆疊掃描，不是含完整符號的 unwind，無法單獨證明破壞記憶體的最初呼叫。
尚需 G HUB 停用與相同種子重跑的對照，未自動更動任何外部程式設定。

## 17:27 新崩潰與 0.4.0.7 修正

來源：使用者提供 `crash-20260922172739.tspack`，以及其中記載的
`dalamud_appcrash_20260922_172732_875_137616.dmp`。
封裝內 `dalamud.log` 為空，`trouble.json` 表示沒有故障收集資料，因此無法從這份封裝確認當時的機制秒數。

- 例外為 `C0000005`，發生於 `ffxiv_dx11.exe+0x3EB1D9`。
- 對應的本機遊戲程式碼函式起點 `+0x3EB1A0` 符合插件 `VfxDataPointers.Dtor` 的 signature。
- 失敗指令為 `call qword ptr [rax+8]`；它先從特效物件 `+0x1C0` 取出 listener，再讀取其虛擬函式表。
- 此次 `RAX=0xBC0000005`，導致讀取無效位址 `0xBC000000D`。
- 原先 SimVfx 只依自己的倒數清理，沒有得知遊戲已自行刪除特效。這是能導致 stale pointer／重複解構的確定程式缺陷，與此次崩潰位置吻合；但不能僅靠這份堆疊排除其他記憶體破壞。

新版攔截原生 VfxData 解構，在釋放前使模擬器持有的 Handle 失效；主動清理也先解除所有權，
避免回呼重入或舊地址重用時誤刪新物件。Fire-and-forget 特效直接交給遊戲管理。
插件卸載時先清理模擬世界，再卸除解構通知 hook。

`tools/VfxValidation` 直接引用實際 SimVfx 原始碼，用替身檢查遊戲先釋放、重複清理、地址重用三種情況。
Release API 13 編譯零警告零錯誤。測試不執行原生 hook；仍需完整重啟遊戲，實測頭標到期、重置與離開。
這份新證據不能歸因於 G HUB；先前的 LED 模組線索並未證實根因。
